# Data Visualization using R and ggplot2

This repository contains my practical work and scripts for learning data programming and visualization using **R** and **RStudio**. 

## 📌 Project Overview
The main objective of this practice was to understand how to handle custom datasets (data frames) and visualize them effectively using the `ggplot2` package. 

## 🛠️ Tech Stack & Libraries Used
* **Language:** R
* **IDE:** RStudio
* **Core Library:** `ggplot2`

## 📊 Key Implementation
* Created custom data structures with specific coordinates (`col1` and `col2`).
* Generated clear line-and-point plots utilizing `geom_line()` and custom `geom_point()` aesthetics.
* Successfully handled parameter updates based on library version specifications.

## 🚀 How to Run the Script
1. Open RStudio.
2. Install ggplot2 if you haven't already:
```R
   install.packages("ggplot2")
